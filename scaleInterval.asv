function [ intervalDat, coeffs ] = scaleInterval( data, interval, coeffs )

    intervalDat = data;
    if nargin < 3
        intervalOld = max(outdata.x) - min(outdata.x);
    else
        
    end
    
end

